/* ============================================================
   Task 5: Student Record System — Classify a Record
   → Write a program to check whether a given student's `marks`
     meets a specific condition using if-else.
   ============================================================ */

let marks = 40;

if (marks >= 40) {
    console.log(`Marks (${marks}) meet the passing condition. Result: PASS`);
} else {
    console.log(`Marks (${marks}) do not meet the passing condition. Result: FAIL`);
}

/* ------------------------------------------------------------
   Expected Sample Output:
   Marks (40) meet the passing condition. Result: PASS
   ------------------------------------------------------------ */
